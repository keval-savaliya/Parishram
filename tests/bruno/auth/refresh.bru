meta {
  name: Refresh Token
  type: http
  seq: 5
}

post {
  url: {{baseUrl}}/api/auth/refresh
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

docs {
  Refresh normally uses the refresh_token cookie. Bearer here is the access token; use the web flow for cookie refresh.
}
